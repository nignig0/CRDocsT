get_filename_component(CRDT_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

list(APPEND CMAKE_MODULE_PATH ${CRDT_CMAKE_DIR})

if(NOT TARGET crdt::crdt)
    include("${CRDT_CMAKE_DIR}/crdtTargets.cmake")
endif()

set(CRDT_LIBRARIES crdt::crdt)
